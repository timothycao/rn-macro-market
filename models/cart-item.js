class CartItem {
  constructor(quantity, productPrice, productTitle, totalAmount) {
    this.quantity = quantity;
    this.productPrice = productPrice;
    this.productTitle = productTitle;
    this.totalAmount = totalAmount;
  }
}

export default CartItem;
